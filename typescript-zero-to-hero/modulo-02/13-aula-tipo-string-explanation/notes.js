/*
let nomeVariavel: TypeAnnotation = valor;

Exemplos:

// Usando o single quotes (aspas simples)
let animal_1: string = 'tiger';
console.log(animal_1);

// Usando o double quotes (aspas duplas)
let animal_2: string = "horse";
console.log(animal_2);

let nomePessoa: string = 'Luis Cesar';
let nomeEmpresa: string = 'Uneel';

// Usando back ticks (crase) para recuperar os valores das variáveis. Ele pode ser usado quando você tem várias variáveis para ser atribuidas dentro do escopo de uma função.
let detalhesFuncionario: string = `${nomePessoa} trabalha na empresa ${nomeEmpresa}.`;
console.log('detalhes: ' + detalhesFuncionario);

Dentro do typescript existem dois tipos de string:
String que é do tipo object e string que é do tipo primitivo
*/
