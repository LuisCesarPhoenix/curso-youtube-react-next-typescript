/*
O typescript é uma linguagem altamente tipada, ou seja, ele irá identificar os tipos das variáveis e das funções.

let nomeVariavel: TypeAnnotation = valor;
function funcao(parametro: Type): Type {
  return parametro;

Exemplo:

let nome: string = 'Glaucia Lemos';
function mostrarNome(nome: string): string {
  return nome;
}

O TypeAnnotation é uma anotação que indica o tipo da variável ou da função.
Ela pode ser uma string, um number, um boolean, um array, um object, um function, etc.

Exemplo:

let num1: number = 10;
let num2: number = 20;
function adicionarNumeros(num1: number, num2: number): number {
  return num1 + num2;
}

console.log(adicionarNumeros(num1, num2));
*/
