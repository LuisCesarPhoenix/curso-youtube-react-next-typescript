/*
Type - Tuple(Tupla)

Vai chegar um momento em que precisaremos armazenar uma coleção de valores de diferentes tipos, porém os arrays ou matrizes
não conseguem lidar com isso.
A tupla representa uma coleção heterogênea de valores (de diferentes tipos), onde cada item é de um tipo diferente.
As tuplas podem ser passadas como parâmetros de função.
A tupla pode ser considerada uma matriz com um número fixo de itens.
Tuplas são aarrays de elemntos fixos. É preciso declarar os valores na mesma ordem que os tipos foram declarados.

Sintaxe:
let nomeTupla = [valor1, valor2, ..., valorN];

Os valores podem ser de qualquer tipo.

- Tuplas simples

let pessoa: [string, string, number];
pessoa = ['Glaucia', 'Lemos', 32];
pessoa = [32, 'Lemos', 'Glaucia']; // Erro de tipagem

- Tuplas com labels
- Usando spread operator
- Listas heterogêneas
- Usando funções

*/
